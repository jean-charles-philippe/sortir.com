


{{ form_start(form)}}

<div class="row ">
        <div class="col-12 col-md-6 p-5">
            {{ form_row(form.name) }}
            {{ form_row(form.vacation_date) }}
            {{ form_row(form.vacation_limitDate) }}
            {{ form_row(form.placeNumber) }}
            {{ form_row(form.duration) }}
            {{ form_row(form.description) }}
        </div>
        <div class="col-12 col-md-6 p-5">
            {{ form_row(form.campus) }}
            {{ form_row(form.city) }}
            {{ form_row(form.location) }}
            {{ form_row(form.street) }}
            {{ form_row(form.cp) }}
            {{ form_row(form.latitude) }}
            {{ form_row(form.longitude) }}
        </div>
</div>


{{ form_end(form) }}

